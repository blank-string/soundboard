import React from 'react'
import PropTypes from 'prop-types'

const Tags = ({updateTags, tags}) => <div>
    Tags
</div>

Tags.propTypes = {
  updateTags: PropTypes.func.isRequired,
  tags: PropTypes.array
}

export default Tags
